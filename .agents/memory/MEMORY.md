- [RMC App Layout Fix](rmc-layout-fix.md) — Mobile header must NOT be a grid/flex child alongside sidebar; keep it above the flex row in a column container.
- [Workbox runtimeCaching urlPattern](workbox-runtimecaching-urlpattern.md) — Workbox RegExp urlPatterns match the full href, not pathname; use a `({url})=>url.pathname.startsWith('/api')` callback or the rule silently no-ops.
- [RMC runtime theming](rmc-theming.md) — single teal Day/Night theme under legacy --gold* keys; accent must be set in theme.tsx + index.css + Landing.css(.ck) + static index.html or a closer layer silently overrides var(--gold).
- [RMC 401 handling](rmc-401-handling.md) — global fetch wrapper clears session+redirects on 401, but only when a token was sent, so login's own 401 isn't hijacked.
- [RMC last-admin guard](rmc-soft-delete-guard.md) — last-admin delete branch is unreachable via real API (self-delete shadows it); test it with a deletedAt+active admin actor and signToken.
- [RMC server tests](rmc-server-tests.md) — `pnpm test` in server/ provisions a UNIQUE per-run `<db>_test_<pid>_<ts>` DB (safe to run concurrently with validation), pushes schema, runs node:test+supertest serially.
- [RMC frontend tests](rmc-frontend-tests.md) — vitest+jsdom+testing-library in rmc-app; `test` validation chains server then rmc-app; mock useSSE, use real ToastProvider to test Layout SSE→toast.
- [RMC lint validation](rmc-lint-validation.md) — react-hooks@7 purity/set-state-in-effect + react-refresh/only-export-components enforced as ERROR; loaders must be non-async promise-chains (no sync setState in effects).
- [Screenshot URL caching](screenshot-url-cache.md) — external_url screenshots are cached per-URL; add a `?cb=` query param to force a fresh capture of a changed page.
- [Canvas presentArtifact id](canvas-present-artifact-id.md) — mockup canvas work uses artifactId `artifacts/mockup-sandbox` (not the slug); recover it from the presentArtifact error's Available-artifacts list; verify preview via curl 200 (screenshots hit auth wall).
- [RMC shift windows](rmc-shift-windows.md) — shifts (esp. night 22:00→06:00) cross midnight; use absolute Date start/end + date-anchored persistence keys, never hour-of-day or isToday.
- [SSE event targeting](sse-event-targeting.md) — emitSSEEvent(event,data,audience?) scopes per-recipient; missing audience silently broadcasts to all.
- [RMC link delete guard](rmc-link-delete-guard.md) — deleting a client/driver is blocked (409) while a non-soft-deleted user still links to it; scope check with isNull(deletedAt).
- [RMC test app mounts](rmc-test-app-mounts.md) — server tests use buildTestApp() (subset of routers), not src/index.ts; new routes 404 unless mounted there too; baseline `pnpm test` has 7 unrelated pre-existing failures.
- [Audit-log canonical reader](audit-log-canonical-reader.md) — all audit UIs read one /api/audit-logs; withTotal=1 is opt-in for the numbered pager only; facets come from /audit-logs/facets.
- [RMC freshness for non-staff](rmc-freshness-nonstaff.md) — client/driver get a pour-by countdown via config-only /positions/freshness-config; never expose the staff-only plant-wide /positions/freshness to them.
- [RMC proof photos](rmc-proof-photo-storage.md) — multiple photos per challan in challan_proof_photos child table storing /objects/ paths (not base64), served as signed GCS URLs from the authed detail endpoint; PUT takes proofPhotos[]+legacy proofPhoto; list carries only hasProofPhoto (EXISTS); stub proofPhotoStore via mock.method.
- [Deploy build type-checks tests](deploy-build-typechecks-tests.md) — deploy build runs tsc over *.test files; test-only type errors (TS6133/TS2322 vi.spyOn/TS2769 enum) block publish; re-verify full build after every merge wave.
- [RMC frontend test flakiness](rmc-frontend-test-flakiness.md) — "passes alone, fails in full run" = ToastProvider 4s timer leak (clear on unmount) or CPU-starved userEvent timeouts (raise global testTimeout); blamed test is usually collateral.
- [RMC JWT_SECRET & Clerk SSO](rmc-jwt-secret-and-clerk-sso.md) — JWT_SECRET required at boot (no fallback); set as SHARED env var, not a user secret; bash shell can't see it (use viewEnvVars); Clerk SSO is browser→/api/auth/clerk token-exchange→legacy JWT.
- [RMC frontend test speed](rmc-frontend-test-speed.md) — use vitest `pool:'threads'` (never `isolate:false`); lazy-import heavy export deps (xlsx/jspdf); judge timing from the validation `test` workflow, not ad-hoc runs.
- [Leaflet markers under Vite](leaflet-vite-markers.md) — Leaflet default marker PNGs break when bundled; every map/marker must supply its own L.icon with absolute/CDN URLs (free OSM+Nominatim stack, no API key).
- [RMC recurring scheduler](rmc-recurring-scheduler.md) — periodic "read due rows then write" loops must claim each row in its own tx with FOR UPDATE SKIP LOCKED + advance schedule in same tx, or boot/interval overlap & crashes double-fire.
- [RMC MyOrders error surfaces](rmc-myorders-error-banner.md) — page-level `error` state is a full-page replacement; per-action failures must use a local dismissible banner, not setError.
- [RMC test gate root-causes](rmc-test-baseline-failures.md) — `pnpm test` is fully green (no standing baseline failures); a red gate is real. Top recurring cause: adding an export to email.ts breaks every `mock.module('../lib/email.js')` stub.
- [RMC test bcrypt cost](rmc-test-bcrypt-cost.md) — hashing goes through hashPassword()/BCRYPT_ROUNDS in lib/password.ts; cost 4 under NODE_ENV=test (vs 10 prod) saves ~35–50s; never hardcode cost or assert hash prefix.
- [Integration files break prod ESM](integration-esm-extensions.md) — replit_integrations/* use extensionless relative imports; fine under tsx dev but crash prod node ESM (ERR_MODULE_NOT_FOUND). Add .js after (re)installing an integration.
- [RMC reports/dashboard tenant scoping](rmc-reports-tenant-scoping.md) — EVERY aggregate over challans/orders/clients (incl. forecast history+booked and fuel-reconciliation JSON+CSV) must append plantScope; helper funcs need an explicit actorPlantId param.
- [drizzle error wrapping](drizzle-error-wrapping.md) — drizzle-orm 0.45 wraps query errors; PG `code`/`constraint` live on `err.cause`, not top-level — read both or 23505 checks silently no-op.
- [seed.ts is fresh-DB-only](seed-fresh-db-only.md) — full seed breaks on a populated DB (onConflictDoNothing → empty returning → homePlant undefined); add row deltas via direct INSERT…ON CONFLICT, not by re-seeding.
- [RMC plant directory bootstrap](rmc-plant-directory-bootstrap.md) — PLANT_DIRECTORY is the single source; ensurePlantDirectory() self-seeds it on every boot (dev restart + prod publish) via onConflictDoNothing on plantCode, since prod DB is read-only to the agent.
- [RMC nearby radius](rmc-nearby-radius.md) — customer plant discovery is gated by a UI search radius (default 40km, options up to 250km); backend /plants/nearby clamps radius to 250km max on the public route.
- [Google Maps short-link coords](gmaps-shortlink-coords.md) — maps.app.goo.gl → CID → webFetch `?cid=` page; exact GPS is the `!1x<lat×1e7>!2x<lng×1e7>` tile params, not the HTML/Nominatim fallback.
- [RMC lead vs verified plants](rmc-lead-verification.md) — `verified` (not `locationVerified`) is the customer-visibility gate; leads (verified=false) never hit /nearby; owner login = plant-scoped user via POST /plants/:id/owner.
- [RMC live plant discovery](rmc-plant-discovery.md) — /plants/discover uses Places API (New) text search, soft-fails 503 w/o key, rate-limited+cached; invalid key=400 API_KEY_INVALID (valid key ~39 chars `AIza…`).
- [RMC plant invites](rmc-plant-invites.md) — customer "Request this plant" → /plants/invite is open to ANY authed user (not staff); de-duped by placeId, status CASE reopens dismissed but never onboarded.
- [RMC phone-OTP customer auth](rmc-phone-otp-auth.md) — customers log in by phone (WhatsApp OTP); placeholder @otp.local email + nullable users.phone partial-unique key; block @otp.local in register + fail dev OTP closed in prod.
- [Staff passwordless + SuperAdmin 2FA](staff-passwordless-auth.md) — staff/owner login is provisioned-only passwordless OTP (password /login→403 useOtp); SuperAdmin 2FA has NO dev fallback (502, no token); billing gate must sit on BOTH /login AND /staff/otp/verify (post-identity, no leak on wrong code).
- [RMC discover shared store](rmc-discover-shared-store.md) — /plants/discover rate limit + Places cache live in Postgres (rate_limit_hits, response_cache) so they hold across instances; buildTestApp has no trust-proxy so X-Forwarded-For can't set req.ip in tests.
- [app_settings leaks across tests](rmc-settings-test-isolation.md) — settings-driven suites must TRUNCATE app_settings in beforeEach; the standard users/plant truncate misses it, so persisted settings (toggles, recipients) silently bleed between tests.
- [Plant-request notify audience](rmc-plant-invite-notify-audience.md) — plant.invite email = active users in chosen roles UNION extra recipients (augment, not replace); roles null=default admin+authority, ''=explicit none; SSE toast now async, await flush in tests.
- [RMC role default routing](rmc-role-default-routing.md) — changing ROLE_DEFAULT_PATH alone won't reroute a role that can already access the old target; the GuardedRoute only redirects DISALLOWED paths, and Login email/OTP handlers had hardcoded setLoc('/').
- [RMC platform-staff guard](rmc-platform-staff-guard.md) — global cross-tenant staff routes (/api/users + global /api/plants) need an isPlatformStaff filter ON TOP of requireRole, else plant-scoped admins (plantId!=null) escalate; per-plant caps need a FOR UPDATE tx.
- [WhatsApp retry queue](whatsapp-retry-queue.md) — transient (5xx/429/network) WhatsApp sends are queued & retried out-of-band via a two-phase LEASE (network call outside any tx); permanent 4xx never queued; retryable flag is the source of truth.
- [WhatsApp Meta provider](whatsapp-meta-provider.md) — sendWhatsAppTemplate is Meta-first→Twilio→dev; Meta needs BOTH phone-id+token to activate; template field holds Meta NAME not Twilio SID; no Meta delivery webhook yet.
- [WhatsApp Meta Graph access](whatsapp-meta-graph-access.md) — from the app server, Meta Graph READS fail "API access blocked" (dev-mode app) but POST send + create-template work; get phone/WABA IDs from user screenshots, not API.
- [WhatsApp Meta OTP login](whatsapp-meta-otp-login.md) — phone-login OTP: code generated+verified LOCALLY, delivered via an approved AUTHENTICATION template; devMode=!isOtpDeliveryConfigured; failed-send cleanup must be hash-scoped; AUTH templates can 400 (subcode 2388185) on sandbox WABAs even when UTILITY works.
- [Phone→client identity boundary](rmc-phone-client-boundary.md) — phone login (OTP/Clerk SMS) → client-role ONLY via resolveCustomerByPhone (non-client = 403, re-applied on the 23505 race re-read too); staff use email/SSO.
- [RMC AI Help Agent](rmc-ai-help-agent.md) — isolation lives in buildAiContext (not the reply); reply-asserting tests MUST unset Gemini env vars in before() or they false-positive once the integration is configured.
- [Gemini integration native-only](gemini-integration-native-only.md) — Replit Gemini v2 sidecar takes ONLY native :generateContent/:streamGenerateContent (x-goog-api-key), never OpenAI /chat/completions; 2.5-flash needs thinkingBudget:0 or empty text.
- [Twilio status-callback signature](twilio-webhook-signature.md) — validate X-Twilio-Signature against the *configured* callback URL (APP_URL/PUBLIC_URL), never the proxy-rewritten request URL; pin APP_URL in tests to reproduce the HMAC.
- [Testing WhatsApp send outcomes](whatsapp-send-test-seam.md) — in a full-app supertest, don't mock.module whatsapp.js (tsx makes .ts/.js the same module, pre-import poisons the mock); set TWILIO_* env + stub global.fetch by status code.
- [WhatsApp resend action](whatsapp-resend.md) — staff re-send re-invokes the same notify* helper (which inserts a fresh row); detect success by newest-row-id != original-id, resent:false means gated off (no template/phone/disabled).
- [AI chat streaming + shared prep](ai-chat-streaming.md) — /ai/chat (JSON) & /ai/chat/stream (SSE) MUST share prepareChat() for the whole security boundary; stream uses chatCompleteStream generator + aiApi.chatStream reader.
- [RMC Nearby remembered location](rmc-nearby-remembered-location.md) — NearbyPlants persists last coords+radius in localStorage; mount bootstrap must defer setState (no sync set-state-in-effect).
- [RMC place-order plant default](rmc-plant-default-priority.md) — Place Order pre-select priority: marketplace handoff/reorder (set plant directly) > pinned default > single-option > last-ordered > blank; /plants/directory + /me/preferred-plant back it.
- [Test-harness helper importable](test-harness-helper-importable.md) — to unit-test pure helpers in scripts/*.mjs from src/test/*.test.ts, pair the .mjs with a sibling .d.mts so tsc resolves types without a rootDir/build error.
- [WhatsApp test env isolation](whatsapp-test-env-isolation.md) — tests asserting the UNCONFIGURED Twilio branch must snapshot+delete TWILIO_* in before() / restore in after(), else real workspace secrets flip the dev-vs-live path.
- [Shared CSV import parser](rmc-shared-csv-parser.md) — plant-import CSV reader is TWO byte-identical copies (server/src/lib/csv.ts + rmc-app/src/lib/csv.ts); update both, keep browser-safe, never cross-import server/ from rmc-app (breaks Android export).
- [RMC Capacitor Android](rmc-capacitor-android.md) — native/web split is env-driven (VITE_API_BASE_URL + VITE_DISABLE_PWA, default empty = web unchanged); Capacitor pinned v7 for Node 20; sharp must be a direct devDep. AAB bundles dist (no server.url) so web publish never reaches testers — rebuild+bump versionCode+reupload per release.
- [RMC brand icons](rmc-brand-icons.md) — favicon/PWA/social/launcher icons are static hand-maintained files, NOT derived from logo-king.png; updating the logo without regenerating public/* makes the domain show the OLD logo.
- [RMC web push](rmc-web-push.md) — order/delivery alerts via email+VAPID web push (SW importScripts); unsubscribe MUST scope deleteSubscription by userId (endpoint-only = IDOR).
- [RMC password reset vs invite](rmc-password-reset.md) — reset shares invite token table via a `kind` col; reset link origin must be config-only (not Host header) & reset must NOT reactivate suspended accounts.
- [RMC plant-owner onboarding](rmc-partner-onboarding.md) — owners self-request via public /plants/partner-request into the plantInvites lead queue (approve-first, synthetic partner:<hex> placeId); 3 landing doors (customer/?staff=1/partner); Back-to-home on every public auth page.
- [WhatsApp SMB /register block](whatsapp-smb-register-block.md) — SMB (Business App) numbers can't /register to Cloud API ("not available for SMB businesses"); WABA GET demanding partner_ids = shared/SMB; needs coexistence Embedded Signup or a fresh number.
- [RMC in-app alert sounds](rmc-alert-sounds.md) — Web Audio chime fires from ToastProvider.showToast for all toasts; must feature-detect AudioContext (jsdom has none) + unlock on first gesture; toggle via lazy useState not effect.
- [RMC prod SPA route whitelist](rmc-spa-route-whitelist.md) — prod server only serves index.html for a HARDCODED SPA_ROUTES set; any new public client route (e.g. /partner) 404s in prod until added there (dev Vite hides this).
- [RMC public share links](rmc-public-share-link.md) — public no-login link MUST hash token + set TTL (per-trip 24h) + return no-PII minimized payload; route mounted before requireAuth; attendance check-in/out gated to non-client roles.
- [WhatsApp WABA account review gates /register](whatsapp-waba-account-review.md) — business_verification=verified ≠ usable; /register needs WABA account_review_status APPROVED + a non-DECLINED display name; SMB-type WABA is a dead-end, only a Cloud API WABA can send templates.
- [Runtime secrets vs viewEnvVars](rmc-env-runtime-vs-viewenvvars.md) — viewEnvVars under-reports secrets the running server has (Twilio/Clerk showed missing but were live); probe endpoints, not viewEnvVars. Customer OTP is Twilio SMS here, not Meta.
- [RMC supplier discovery](rmc-supplier-discovery.md) — authority-only Places multi-category supplier map+invite; Leaflet/OSM display but real Places DATA, ONE all-category fetch + client chip filter (never re-bill per category).
- [lucide brand icons removed](lucide-brand-icons-removed.md) — lucide-react dropped Instagram/Youtube/Facebook/Linkedin/Github; use inline simple-icons SVG paths, generic glyphs still fine.
- [RMC SMTP recovery & sender](rmc-smtp-recovery.md) — SMTP_SYNC_FROM_ENV=1 boot-syncs env→app_settings + verifies; boot log is the only probe; sender is GoDaddy mailbox notification@trackmyrmc.com (no app password).
- [Dependency audit covers all lockfiles](dependency-audit-lockfiles.md) — audit scans every pnpm-lock (root/server/rmc-app/mockup) AND the root npm package-lock.json; regenerate the npm lock separately or the advisory stays red; use scoped `pkg@<ver` pnpm overrides for transitive fixes.
- [Mockup subagent escaped template literals](mockup-escaped-template-literals.md) — DESIGN-subagent-generated .tsx can ship className template literals with backslash-escaped backticks/`\${` that pass build/curl but throw vite:react-babel "Expecting Unicode escape sequence" at browser-compile; grep '\\`' after every subagent fan-out.
