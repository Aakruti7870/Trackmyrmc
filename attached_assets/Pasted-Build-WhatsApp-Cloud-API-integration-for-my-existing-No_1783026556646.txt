Build WhatsApp Cloud API integration for my existing Node.js Replit app TrackMyRMC.

Important:
Use my already registered WhatsApp Business API number under KB Construction.
Do NOT create a new WhatsApp Business Account.
Do NOT use the duplicate Concrete King RMC WABA.
Use Phone Number ID: 1281365125049836

Create these features:

1. Environment variables
Add support for:
WHATSAPP_TOKEN
PHONE_NUMBER_ID=1281365125049836
VERIFY_TOKEN=trackmyrmc_verify_7870
GRAPH_API_VERSION=v20.0

2. Install required package
Use axios if not already installed.

3. Webhook verification route
Create GET /webhook
It must verify Meta webhook using:
hub.mode
hub.verify_token
hub.challenge
If token matches VERIFY_TOKEN, return challenge.
Otherwise return 403.

4. Webhook receive route
Create POST /webhook
It must:
- Receive incoming WhatsApp messages
- Log full payload
- Extract customer mobile number
- Extract customer name if available
- Extract text message
- Store received messages in database/table/collection named whatsapp_messages
- Always return HTTP 200 quickly

Message table fields:
id
wa_message_id
from_number
customer_name
message_type
message_text
direction: inbound/outbound
status
raw_payload
created_at

5. Send message API
Create POST /api/whatsapp/send
Body:
{
  "to": "917498286760",
  "message": "Hello from TrackMyRMC"
}

It must send text message using:
POST https://graph.facebook.com/v20.0/{PHONE_NUMBER_ID}/messages

Headers:
Authorization: Bearer WHATSAPP_TOKEN
Content-Type: application/json

Payload:
{
  "messaging_product": "whatsapp",
  "to": to,
  "type": "text",
  "text": { "body": message }
}

Store outbound message in whatsapp_messages.

6. Template message API
Create POST /api/whatsapp/send-template
Body:
{
  "to": "91XXXXXXXXXX",
  "template_name": "order_update",
  "language": "en_US"
}

Send WhatsApp approved template message.

7. Order update automation
Create helper function sendOrderUpdateWhatsApp(order)
Send WhatsApp message for:
- Order Confirmed
- Production Started
- Transit Mixer Dispatched
- Concrete Delivered
- Invoice Generated

Message must include:
TrackMyRMC / Concrete King branding
Order ID
Grade
Quantity
Plant name
Truck number
Driver mobile
Status

8. WhatsApp Inbox API
Create:
GET /api/whatsapp/chats
GET /api/whatsapp/messages/:phone
POST /api/whatsapp/reply

These should allow frontend to show customer-wise chat list and messages.

9. Admin frontend page
Create WhatsApp Manager page with:
- Chat list
- Selected customer chat
- Message input
- Send button
- Order-linked quick reply buttons:
  Order Confirmed
  Production Started
  TM Dispatched
  Delivered
  Send Invoice

Use existing app styling. Do not break existing routes.

10. Security
Never expose WHATSAPP_TOKEN on frontend.
All WhatsApp API calls must happen from backend only.
Validate phone numbers.
Return clean JSON errors.

11. Meta webhook setup instructions
After coding, tell me the webhook URL:
https://MY-REPLIT-APP.replit.app/webhook

Verify token:
trackmyrmc_verify_7870

Subscribe webhook field:
messages

Deliver complete working code changes.