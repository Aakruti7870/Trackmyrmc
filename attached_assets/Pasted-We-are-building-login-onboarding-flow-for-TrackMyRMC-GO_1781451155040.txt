We are building login + onboarding flow for TrackMyRMC / GOLD-e Tech RMC system.

Create complete role-based authentication, database schema, tables, permissions, validation, and error-free compact implementation.

Main access flow:

SUPER OWNER:
krushnabade54@gmail.com
support@goldetech.com

These 2 emails are the master accounts.

Important:
These emails should also login through normal Staff Login screen only, but after login they must see “Plant Owner Onboarding / Issue Owner Access” dashboard.

Flow:
Super Owner → issues login/access to Plant Owner
Plant Owner → issues Admin login
Admin → issues Operator, Supervisor, Dispatcher, Driver/staff login
User/Customer → free registration and place order without restriction

Customer/User rules:

- Any customer/user can register freely.
- Customer can place RMC order.
- No approval required for customer account.
- Customer login must be separate from staff/plant permission logic.

RMC Plant login rules:

- Plant Owner login is not public.
- Plant owner access must be issued only by Super Owner.
- Add Plant Owner Onboarding Form.
- In future, Super Owner can onboard more plant owners and assign plant access/permissions.
- Plant Owner can create/suspend Admin login.
- Admin can add/suspend/delete fraud staff logins.
- Admin can add driver and all other staff.
- All staff can perform common plant activities like challan creation, add driver, dispatch activity, etc., based on assigned permissions.

Login account limits per plant:

- Owner: 1
- Admin: 2
- Operator: 2
- Supervisor: 2
- Dispatcher: 1
- Driver: can be added by Admin and authorized staff

Registration restrictions:

- Only Customer/User registration is open.
- Plant Owner/Admin/Operator/Supervisor/Dispatcher/Driver cannot self-register publicly.
- Staff accounts must be created/invited by authorized higher role only.
- Admin login must be allowed only for registered/approved email ID or mobile number.

Required database/schema:
Create tables for:

1. users
2. roles
3. plants
4. plant_owner_access
5. staff_accounts
6. staff_permissions
7. onboarding_requests / owner_onboarding
8. login_sessions
9. audit_logs
10. suspended_accounts

Add fields:

- id
- name
- email
- mobile
- password_hash/auth_provider_id
- role
- plant_id
- created_by
- status: active / suspended / pending / rejected
- permissions JSON
- created_at
- updated_at
- suspended_by
- suspension_reason

Permissions:

- Super Owner: full system control, onboard plant owner, suspend anyone
- Plant Owner: manage assigned plant, create/suspend Admin
- Admin: manage staff, delete fraud users, add drivers, create challan, dispatch operations
- Operator/Supervisor/Dispatcher: plant operations, challan and dispatch related activities
- Driver: assigned delivery/activity access only
- Customer/User: register, place order, view order status

Required pages:

1. Customer Register/Login
2. Staff Login
3. Super Owner Dashboard
4. Plant Owner Onboarding Form
5. Plant Owner Dashboard
6. Admin Staff Management
7. Suspend/Activate Login Page
8. Role Permission Management
9. Audit Log Page

Validation:

- Enforce role limits per plant.
- Prevent duplicate email/mobile.
- Prevent unauthorized role creation.
- Prevent public registration for staff roles.
- Staff login should fail if status is suspended/pending.
- Add proper error messages.
- Add audit log for every user created, suspended, activated, deleted, permission changed.

Also check full project for errors:

- Fix schema errors
- Fix auth errors
- Fix route errors
- Fix UI flow errors
- Make compact, fast loading, mobile responsive, and quick responding version.
- Keep code clean and production-ready.